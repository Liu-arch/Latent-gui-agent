# Sanitized core package versions observed in the source qwen3_agentnet environment.
# Install the PPU vendor's PyTorch build before this file.
# Torch, CUDA, NCCL, Triton, vLLM, FlashInfer, and unrelated utilities are omitted.

accelerate==1.13.0
einops==0.8.1
matplotlib==3.10.9
numpy>=1.26,<2.1
peft==0.19.1
pillow==11.3.0
qwen-vl-utils==0.0.14
safetensors==0.6.2
sentencepiece==0.2.1
tiktoken==0.11.0
tokenizers==0.22.1
tqdm==4.67.1
transformers==4.57.6
